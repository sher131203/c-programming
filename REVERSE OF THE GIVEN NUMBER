#include <stdio.h>
int main()
{
  int Num=10245;
  int rev_Num = 0;
  int rem;
 
   while (Num != 0)
   {
   rem = Num % 10;
   rev_Num = rev_Num * 10 + rem;
    Num = Num/10;

  }

  printf("The reversed number is: %d", rev_Num);

  return 0;
}
